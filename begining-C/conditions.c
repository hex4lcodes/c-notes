//ELIGIBILITY 

#include <stdio.h>

int main() {
    //age checker - NOT USING IF STATEMENTS

    int age;

    age = 12;

    age > 18 ? printf("You are old enough to vote!") : printf("You cannot vote!"); //conditional ternary operator

    //conditional statement ? if true : if false
    

   return 0; 
}
